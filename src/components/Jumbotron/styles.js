import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  profile: {
    height: 'auto',
    width: '325px',
    borderRadius: '35px',
    objectFit: 'cover',
    objectPosition: '50% 50%'
  }
}));